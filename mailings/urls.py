from django.urls import path

# from .views import

app_name = 'mailings'

urlpatterns = [
    # path('mailings/', VideoListAPIView.as_view(), name='videos_list'),
    # path('mailings/<int:pk>/update', ThemeRetrieveUpdateAPIView.as_view(), name='videos_update'),
]
